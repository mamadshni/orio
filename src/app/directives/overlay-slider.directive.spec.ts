import { OverlaySliderDirective } from './overlay-slider.directive';

describe('OverlaySliderDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlaySliderDirective();
    expect(directive).toBeTruthy();
  });
});
